/**
 * Web stub for @react-native-firebase/app
 * Firebase native modules are not available on web.
 * This stub prevents the bundle from crashing.
 */
const firebase = {
  app: () => ({}),
  apps: [],
  initializeApp: () => ({}),
};

module.exports = firebase;
module.exports.default = firebase;
